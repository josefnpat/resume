This is Josef Patoprsty's Open Source Résumé.

This résumé builds itself:

* Data stored in JSON format (e.g. `~/versions/aaa.json`)
* Dynamically rendered in PHP or LuaJIT
* Styled in CSS (`~/style.css`)

Dependencies
============

* LuaJIT
* Git

Building
========

Run `make aaa`
